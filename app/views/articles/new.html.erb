<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        body { font-family: 'Poppins', sans-serif; }
    </style>
</head>
<body class="bg-gray-50">

    <main>
        <section class="px-4 py-24 mx-auto max-w-7xl">
            <h1 class="mb-8 text-3xl font-extrabold leading-tight text-amber-600">Créer un nouvel article</h1>

            <%= form_with(model: @article, local: true, class: "space-y-6") do |form| %>
                <% if @article.errors.any? %>
                    <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4">
                        <h2 class="font-bold text-red-600"><%= pluralize(@article.errors.count, "erreur") %> ont empêché cet article d'être enregistré:</h2>
                        <ul class="mt-2 text-red-600">
                            <% @article.errors.full_messages.each do |message| %>
                                <li><%= message %></li>
                            <% end %>
                        </ul>
                    </div>
                <% end %>

                <div class="field">
                    <%= form.label :title, class: "block text-gray-700 font-bold mb-2" %>
                    <%= form.text_field :title, class: "w-full p-3 border border-gray-300 rounded-md focus:ring focus:ring-amber-200" %>
                </div>

                <div class="field">
                    <%= form.label :content, class: "block text-gray-700 font-bold mb-2" %>
                    <%= form.text_area :content, class: "w-full p-3 border border-gray-300 rounded-md focus:ring focus:ring-amber-200", rows: 10 %>
                </div>

                <div class="actions">
                    <%= form.submit 'Enregistrer l\'article', class: "px-6 py-2 bg-amber-600 text-white rounded-full hover:bg-amber-700 transition duration-300" %>
                </div>
            <% end %>

            <%= link_to 'Retour', articles_path, class: "mt-6 inline-block px-6 py-2 bg-gray-500 text-white rounded-full hover:bg-gray-600 transition duration-300" %>
        </section>
    </main>

    <footer class="bg-gray-800 text-white py-10">
        <div class="container mx-auto px-6 text-center">
            <p>&copy; 2024 Constructec. Tous droits réservés.</p>
        </div>
    </footer>

</body>
</html>
