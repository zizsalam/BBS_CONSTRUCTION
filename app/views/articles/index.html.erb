<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Constructec - Blog</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Poppins', sans-serif; }
  </style>
</head>
<body class="bg-gray-50">
  <main>
    <section class="px-4 py-24 mx-auto max-w-7xl">
      <h2 class="mb-2 text-3xl font-extrabold leading-tight text-amber-600">Blog Constructec</h2>
      <p class="mb-12 text-lg text-gray-500">Découvrez les dernières tendances et innovations dans le domaine de la construction au Sénégal.</p>

      <div class="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3">
        <% @articles.each do |article| %>
          <article class="bg-white rounded-lg shadow-md overflow-hidden transition-transform duration-300 hover:transform hover:scale-105">
            <div class="h-48 bg-gray-200">
              <% if article.image.attached? %>
                <%= image_tag article.image, class: "w-full h-full object-cover" %>
              <% else %>
                <div class="w-full h-full flex items-center justify-center text-gray-400">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                </div>
              <% end %>
            </div>
            <div class="p-6">
              <h3 class="text-xl font-semibold mb-2 text-gray-900"><%= article.title %></h3>
              <% if article.try(:published_at).present? %>
                <p class="text-sm text-gray-500 mb-3"><%= article.published_at.strftime("%d %B %Y") %></p>
              <% end %>
              <p class="text-gray-600 mb-4">
                <%= truncate(article.body.to_s, length: 120, separator: ' ') %>
              </p>
              <%= link_to article_path(article), class: "inline-block bg-amber-600 text-white px-4 py-2 rounded-full hover:bg-amber-700 transition duration-300" do %>
                Lire la suite
              <% end %>
            </div>
          </article>
        <% end %>
      </div>

      <% if @articles.empty? %>
        <div class="text-center py-12">
          <p class="text-gray-500 text-lg">Aucun article disponible pour le moment.</p>
        </div>
      <% end %>
    </section>
  </main>

  <footer class="bg-gray-800 text-white py-10">
    <div class="container mx-auto px-6 text-center">
      <p>&copy; <%= Time.current.year %> Constructec. Tous droits réservés.</p>
    </div>
  </footer>
</body>
</html>
