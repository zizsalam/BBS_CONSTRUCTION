<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @article.title %> - Constructec</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Poppins', sans-serif; }
  </style>
</head>
<body class="bg-gray-50">

  <main>
    <section class="px-4 py-16 mx-auto max-w-5xl">
      <div class="mb-12">
        <h1 class="text-4xl font-extrabold text-gray-900"><%= @article.title %></h1>
        <p class="mt-4 text-sm text-gray-500">
          Publié par <span class="text-gray-900 font-medium">Constructec</span>
          <% if @article.try(:published_at).present? %>
            • <%= @article.published_at.strftime("%d %B %Y") %>
          <% end %>
        </p>

        <div class="mt-6 mb-8">
          <% if @article.image.attached? %>
            <%= image_tag @article.image, class: "object-cover w-full h-[450px] rounded-lg shadow-lg", alt: @article.title %>
          <% else %>
            <div class="w-full h-[450px] bg-gray-200 rounded-lg flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
            </div>
          <% end %>
        </div>

        <article class="prose max-w-none mt-8">
          <% if @article.body.present? %>
            <%= markdown(@article.body) %>
          <% else %>
            <p class="text-gray-500 italic">Contenu non disponible</p>
          <% end %>
        </article>
      </div>

      <div class="mt-12">
        <%= link_to "Retour au blog", articles_path, class: "inline-block bg-amber-600 text-white px-6 py-2 rounded-full hover:bg-amber-700 transition duration-300" %>
      </div>
    </section>
  </main>

  <footer class="bg-gray-800 text-white py-10">
    <div class="container mx-auto px-6 text-center">
      <p>&copy; <%= Time.current.year %> Constructec. Tous droits réservés.</p>
    </div>
  </footer>
</body>
</html>
