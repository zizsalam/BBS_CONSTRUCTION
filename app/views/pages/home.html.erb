<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Constructec - Votre Partenaire de Construction</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Poppins', sans-serif; }
    </style>
</head>

<body class="bg-gray-50">
    <main>
        <!-- Hero Section -->
        <section class="bg-gradient-to-r from-amber-600 to-amber-400 text-white py-20">
            <div class="container mx-auto px-6 text-center">
                <h1 class="text-4xl md:text-6xl font-bold mb-6">Construisez en toute confiance.</h1>
                <p class="text-xl mb-12">Avec Constructec, bénéficiez d'une consultation architecturale et design gratuite. Transparence totale à chaque étape : pas de frais cachés, pas de surprises. Simplifiez vos projets avec nous.</p>
                <a href="<%= new_consultation_path %>" class="bg-white text-amber-600 py-3 px-8 rounded-full font-semibold hover:bg-gray-200 transition duration-300">Consultation gratuite</a>
            </div>
        </section>

        <!-- Services Section -->
        <section class="px-4 py-24 mx-auto max-w-7xl">
            <h2 class="text-3xl font-bold text-center mb-12">Nos Services</h2>
            <div class="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3">
                <!-- Service 1 -->
                <div class="bg-white rounded-lg shadow-md overflow-hidden transition-transform duration-300 hover:transform hover:scale-105">
                    <div class="h-48 bg-gray-200 flex items-center justify-center">
                        <svg class="w-12 h-12 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                        </svg>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-semibold mb-2 text-amber-700">Construction Résidentielle</h3>
                        <p class="text-gray-600 mb-4">Des maisons sur mesure qui reflètent votre style de vie unique.</p>
                    </div>
                </div>
                <!-- Service 2 -->
                <div class="bg-white rounded-lg shadow-md overflow-hidden transition-transform duration-300 hover:transform hover:scale-105">
                    <div class="h-48 bg-gray-200 flex items-center justify-center">
                        <svg class="w-12 h-12 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01"></path>
                        </svg>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-semibold mb-2 text-amber-700">Projets Commerciaux</h3>
                        <p class="text-gray-600 mb-4">Des espaces fonctionnels pour faire prospérer votre entreprise.</p>
                    </div>
                </div>
                <!-- Service 3 -->
                <div class="bg-white rounded-lg shadow-md overflow-hidden transition-transform duration-300 hover:transform hover:scale-105">
                    <div class="h-48 bg-gray-200 flex items-center justify-center">
                        <svg class="w-12 h-12 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                        </svg>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-semibold mb-2 text-amber-700">Rénovations</h3>
                        <p class="text-gray-600 mb-4">Donnez une nouvelle vie à vos espaces existants avec notre expertise.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Blog Section -->
        <section class="px-4 py-24 mx-auto max-w-7xl">
            <h2 class="text-3xl font-bold text-center mb-12">À la Une de notre Blog</h2>

            <% if @articles.empty? %>
                <div class="text-center py-12">
                    <p class="text-gray-500 text-lg">Aucun article disponible pour le moment.</p>
                </div>
            <% else %>
                <div class="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3">
                    <% @articles.each do |article| %>
                        <article class="bg-white rounded-lg shadow-md overflow-hidden transition-transform duration-300 hover:transform hover:scale-105">
                            <div class="p-6">
                                <h3 class="text-xl font-semibold mb-2 text-gray-900"><%= article.title %></h3>
                                <% if article.try(:published_at).present? %>
                                    <p class="text-sm text-gray-500 mb-3"><%= article.published_at.strftime("%d %B %Y") %></p>
                                <% end %>
                                <p class="text-gray-600 mb-4">
                                    <%= truncate(strip_tags(article.body.to_s), length: 120, separator: ' ') %>
                                </p>
                                <%= link_to "Lire plus", article_path(article), class: "inline-block bg-amber-600 text-white px-4 py-2 rounded-full font-semibold hover:bg-amber-500 transition duration-300" %>
                            </div>
                        </article>
                    <% end %>
                </div>
            <% end %>
        </section>

    </main>

    <footer class="bg-gray-800 text-white py-8">
        <div class="container mx-auto px-8 text-center">
            <p>&copy; 2024 Constructec Sénégal. Tous droits réservés.</p>
        </div>
    </footer>
</body>
</html>
