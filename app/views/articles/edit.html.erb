<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier l'article - Constructec</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Poppins', sans-serif; }
    </style>
</head>
<body class="bg-gray-50">
    <header class="bg-white shadow-lg">
        <nav class="container mx-auto px-6 py-4 flex justify-between items-center">
            <div class="text-3xl font-bold text-amber-600">Constructec</div>
            <div class="hidden md:flex space-x-6">
                <a href="home.html" class="text-gray-800 hover:text-amber-500">Accueil</a>
                <a href="services.html" class="text-gray-800 hover:text-amber-500">Services</a>
                <a href="about.html" class="text-gray-800 hover:text-amber-500">À propos</a>
                <a href="blog.html" class="text-gray-800 hover:text-amber-500">Blog</a>
            </div>
        </nav>
    </header>

    <main class="px-4 py-16 mx-auto max-w-5xl">
        <h1 class="text-3xl font-bold text-gray-900 mb-8">Modifier l'article</h1>

        <%= form_with(model: @article, local: true, class: "space-y-8") do |form| %>
            <% if @article.errors.any? %>
                <div id="error_explanation" class="bg-red-100 text-red-700 p-4 rounded-lg">
                    <h2 class="font-bold"><%= pluralize(@article.errors.count, "erreur") %> a empêché la sauvegarde de l'article :</h2>
                    <ul class="list-disc pl-5 mt-2">
                        <% @article.errors.full_messages.each do |message| %>
                            <li><%= message %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <div class="space-y-4">
                <div class="field">
                    <%= form.label :title, class: "block text-gray-700 font-semibold" %>
                    <%= form.text_field :title, class: "w-full p-2 border border-gray-300 rounded-lg focus:ring-amber-500 focus:border-amber-500" %>
                </div>

                <div class="field">
                    <%= form.label :content, class: "block text-gray-700 font-semibold" %>
                    <%= form.text_area :content, class: "w-full p-2 border border-gray-300 rounded-lg focus:ring-amber-500 focus:border-amber-500" %>
                </div>
            </div>

            <div class="actions mt-8">
                <%= form.submit 'Sauvegarder', class: "bg-amber-600 text-white px-6 py-2 rounded-full hover:bg-amber-700 transition duration-300" %>
            </div>
        <% end %>

        <div class="mt-8">
            <%= link_to 'Retour', articles_path, class: 'bg-gray-200 text-gray-700 px-6 py-2 rounded-full hover:bg-gray-300 transition duration-300' %>
        </div>
    </main>

    <footer class="bg-gray-800 text-white py-10 mt-16">
        <div class="container mx-auto px-6 text-center">
            <p>&copy; 2024 Constructec. Tous droits réservés.</p>
        </div>
    </footer>
</body>
</html>
